#include<stdio.h>
/**
 * main -prints the output by using the put instead of printf
 *
 * Return -always (0)
 *
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return(0);
}
